import authorization from "./authorization.action-types";
import appSettings from "./app-settings.action-types";
import application from "./application.action-types"

export default { authorization, appSettings, application }; 
